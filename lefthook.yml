pre-commit:
  parallel: true
  commands:
    secrets:
      run: gitleaks protect --staged --redact

commit-msg:
  commands:
    conform:
      run: conform enforce --config ~/.config/conform/.conform.yaml --commit-msg-file {1}
